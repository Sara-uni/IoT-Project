version: "3.1"

intents:
  - temperature
  - light
  - noise

actions:
  - send_temperature
  - send_light
  - send_noise
  - send_repeat

# session_config:
#   session_expiration_time: 60
#   carry_over_slots_to_new_session: true
